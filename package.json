{
  "name": "md-image-hosting",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild index.js --bundle --minify --platform=node --target=node20 --outfile=dist/image-syntax-replacer.js",
    "hello": "NODE_ENV=prod node test.js"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.654.0",
    "@aws-sdk/credential-providers": "^3.654.0"
  },
  "devDependencies": {
    "esbuild": "^0.23.1"
  }
}
